### Persona-Specific Task Generation — Frontend Engineer

Focus task breakdown on:
- **UI Components**: Each component gets component creation task + test task + story task
- **State Management**: State setup tasks, actions/reducers tasks, selectors tasks
- **Routing**: Route configuration tasks, navigation guard tasks, lazy loading tasks
- **Forms**: Form component tasks, validation tasks, submission handling tasks
- **API Integration**: API client setup tasks, data fetching tasks, error handling tasks
- **Styling**: Theme setup tasks, responsive design tasks, accessibility tasks
- **User Experience**: Loading state tasks, error state tasks, empty state tasks
- **Performance**: Code splitting tasks, bundle optimization tasks, lazy loading tasks
- **Accessibility**: ARIA attributes tasks, keyboard navigation tasks, screen reader tasks
- **Testing Priorities**: Component tests first, integration tests, then E2E tests
- **Dependencies**: Design system before components, components before pages, pages before routing

Break down complex components into subtasks:
- Component structure subtask
- Props interface subtask
- Event handlers subtask
- Styling subtask
- Accessibility subtask
- Unit tests subtask

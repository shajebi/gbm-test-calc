### Persona-Specific Task Generation — Backend Engineer

Focus task breakdown on:
- **API Endpoints**: Each endpoint gets contract test task + implementation task
- **Database Layer**: Schema migration tasks, model creation tasks, repository pattern tasks
- **Service Layer**: Business logic tasks, validation tasks, transaction management
- **Integration**: External API integration tasks, message queue tasks, caching layer tasks
- **Performance**: Database query optimization tasks, API response time validation tasks
- **Error Handling**: Error scenarios for each endpoint, proper status codes, error messages
- **Security**: Authentication tasks, authorization tasks, input validation, SQL injection prevention
- **Data Validation**: Request validation tasks, response serialization tasks
- **Testing Priorities**: Contract tests first, then integration tests, then unit tests
- **Dependencies**: Database must be ready before services, services before endpoints

Break down complex endpoints into subtasks:
- Request validation subtask
- Business logic subtask
- Database interaction subtask
- Response formatting subtask
- Error handling subtask
